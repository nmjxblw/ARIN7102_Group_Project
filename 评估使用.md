# 端到端评估指南

本文档说明如何运行药物推荐系统的端到端评估。

## 前置条件

### 1. Python 环境
- Python 3.9+ (推荐使用 conda 环境)
- 确保使用的是 `pytorch_gpu` 环境或包含 PyTorch 的环境

### 2. 依赖安装

```bash
# 使用 conda 环境
conda activate pytorch_gpu

# 安装必要依赖
pip install fastapi uvicorn transformers sentence-transformers faiss-cpu --user

# 如果遇到 Python 3.9 类型提示兼容性问题
pip install eval_type_backport --user
```

**注意**: 如果 C 盘空间不足导致 pip 安装失败，可以使用 `--user` 选项安装到用户目录，或使用 conda：
```bash
conda install -n pytorch_gpu fastapi uvicorn transformers sentence-transformers -y -c conda-forge
```

### 3. 数据文件准备

确保以下文件存在：
- `match_data_preprocessing/data/enhanced_drug_table_v1_structured.csv` - 增强药物表
- `data/eval_dataset_llm.json` - 评估数据集（如不存在需先生成）

---

## 步骤一：生成评估数据集

如果 `data/eval_dataset_llm.json` 不存在，需要先使用 LLM 生成：

```bash
cd app/evaluation

# 使用 DeepSeek API 生成评估数据集
D:\miniconda3\envs\pytorch_gpu\python.exe generate_eval_dataset_llm.py ^
    --input-csv "../../match_data_preprocessing/data/enhanced_drug_table_v1_structured.csv" ^
    --output-json "../../data/eval_dataset_llm.json" ^
    --num-samples 100 ^
    --min-diseases 1 ^
    --min-symptoms 2 ^
    --model "deepseek-v3-2-251201" ^
    --base-url "https://ark.cn-beijing.volces.com/api/coding/v3"
```

**重要参数说明**:
| 参数 | 说明 |
|------|------|
| `--input-csv` | 增强药物表路径 |
| `--output-json` | 输出评估数据集路径 |
| `--num-samples` | 生成样本数量 |
| `--model` | **必须指定**，LLM 模型名称 |
| `--base-url` | **必须指定**，API 端点 |

---

## 步骤二：运行端到端评估

```bash
cd app

D:\miniconda3\envs\pytorch_gpu\python.exe -m evaluation.run_evaluation ^
    --eval-dataset "../data/eval_dataset_llm.json" ^
    --k-values 5 10 20 ^
    --output "../data/eval_results.json"
```

**参数说明**:
| 参数 | 说明 |
|------|------|
| `--eval-dataset` | 评估数据集路径 |
| `--k-values` | 计算 Precision@K, Recall@K 的 K 值列表 |
| `--output` | 评估结果输出路径（可选） |

---

## 评估指标

评估脚本会计算以下指标：

| 指标 | 说明 |
|------|------|
| `precision@k` | 前 K 个推荐中相关药物的比例 |
| `recall@k` | 前 K 个推荐覆盖了多少相关药物 |
| `hit@k` | 前 K 个推荐中是否包含至少一个相关药物 |
| `mrr` | Mean Reciprocal Rank，第一个相关药物排名的倒数均值 |
| `ndcg@k` | Normalized Discounted Cumulative Gain |

---

## 潜在问题及解决方案

### 问题 1：类型提示兼容性错误

**错误信息**:
```
TypeError: unsupported operand type(s) for |: 'type' and 'NoneType'
```

**原因**: 代码使用了 Python 3.10+ 的类型提示语法（如 `str | None`），但运行环境是 Python 3.9

**解决方案**:
```bash
pip install eval_type_backport --user
```

如果仍有问题，在报错文件顶部添加：
```python
from __future__ import annotations
```

---

### 问题 2：LLM API 调用失败 (UnsupportedModel)

**错误信息**:
```
Error code: 404 - {'error': {'code': 'UnsupportedModel', 'message': 'The gpt-3.5-turbo model does not support...'}}
```

**原因**: 脚本默认使用 `gpt-3.5-turbo`，但 API 端点不支持该模型

**解决方案**: 必须显式指定 `--model` 和 `--base-url` 参数：
```bash
--model "deepseek-v3-2-251201" --base-url "https://ark.cn-beijing.volces.com/api/coding/v3"
```

---

### 问题 3：pip 安装失败 - 磁盘空间不足

**错误信息**:
```
ERROR: Could not install packages due to an OSError: [Errno 28] No space left on device
```

**解决方案**:
1. 清理 C 盘空间
2. 使用 `--user` 选项安装到用户目录
3. 使用 conda 安装（会安装到 conda 环境所在磁盘）：
   ```bash
   conda install -n pytorch_gpu <package_name> -y -c conda-forge
   ```

---

### 问题 4：权限拒绝错误

**错误信息**:
```
ERROR: Could not install packages due to an OSError: [WinError 5] 拒绝访问
```

**解决方案**: 使用 `--user` 选项：
```bash
pip install <package_name> --user
```

---

### 问题 5：模型下载缓慢或失败

**原因**: 首次运行时需要从 Hugging Face 下载模型（PubMedBERT, Cross-Encoder 等）

**解决方案**:
1. 确保网络通畅
2. 设置 Hugging Face 镜像（可选）：
   ```bash
   set HF_ENDPOINT=https://hf-mirror.com
   ```
3. 预先下载模型到本地

---

### 问题 6：CUDA 内存不足

**错误信息**:
```
RuntimeError: CUDA out of memory
```

**解决方案**:
1. 减小 batch_size
2. 使用 CPU 模式：设置环境变量 `CUDA_VISIBLE_DEVICES=""`

---

## 环境变量配置

在 `.env` 文件中配置（或设置环境变量）：

```env
OPENAI_API_KEY=your_api_key
OPENAI_BASE_URL=https://ark.cn-beijing.volces.com/api/coding/v3
OPENAI_MODEL=deepseek-v3-2-251201
```

---

## 完整运行示例

```bash
# 1. 激活环境
conda activate pytorch_gpu

# 2. 进入项目目录
cd D:\7102project\ARIN7102_Group_Project

# 3. 生成评估数据集（如需要）
cd app/evaluation
D:\miniconda3\envs\pytorch_gpu\python.exe generate_eval_dataset_llm.py ^
    --input-csv "../../match_data_preprocessing/data/enhanced_drug_table_v1_structured.csv" ^
    --output-json "../../data/eval_dataset_llm.json" ^
    --num-samples 100 ^
    --model "deepseek-v3-2-251201" ^
    --base-url "https://ark.cn-beijing.volces.com/api/coding/v3"

# 4. 运行评估
cd ..
D:\miniconda3\envs\pytorch_gpu\python.exe -m evaluation.run_evaluation ^
    --eval-dataset "../data/eval_dataset_llm.json" ^
    --k-values 5 10 20 ^
    --output "../data/eval_results.json"
```

---

## 输出结果

评估完成后，结果保存在 `data/eval_results.json`：

```json
{
  "metrics": {
    "precision@5": 0.xxxx,
    "precision@10": 0.xxxx,
    "precision@20": 0.xxxx,
    "recall@5": 0.xxxx,
    "recall@10": 0.xxxx,
    "recall@20": 0.xxxx,
    "hit@5": 0.xxxx,
    "hit@10": 0.xxxx,
    "hit@20": 0.xxxx,
    "mrr": 0.xxxx,
    "ndcg@5": 0.xxxx,
    "ndcg@10": 0.xxxx,
    "ndcg@20": 0.xxxx
  },
  "num_queries": 100,
  "k_values": [5, 10, 20]
}
```
